# AI Spy - 管理后台产品需求文档 (Admin Panel PRD)

## 1. 背景与目标 (Background & Goals)
随着 AI Spy 客户端 H5 与商业化支付流程的逐步完善，运营与产品团队急需一个数据可视化窗口来实时监控平台健康度、商业转化效果以及内容生态（AI 虚假内容的拦截情况）。

**核心目标：**
- **建立核心数据看板**：使团队能够实时“看清”业务现状（拉新、营收、使用频次）。
- **用户与订单追踪**：提供客诉排查工具，能快速定位用户账单及请求历史。
- **内容趋势监控**：监控平台检测出的 `is_fake` 比例，为模型优化及市场营销提供数据支撑。

**成功指标：**
- 内部团队响应客诉时间缩短 50%。
- 实现核心 KPI（日新增、日充值、检测失败率）的秒级图表透视。

---

## 2. 用户故事 (User Stories)

### 2.1 核心数据洞察
- **US1**: 作为**产品/运营负责人**，我想要**在登录后台的第一时间看到四大核心指标（总用户数、当月总收入、总检测次数、虚假内容检出率）**，以便于**我随时掌握平台整体商业变现进度与业务水位**。
- **US2**: 作为**数据分析师**，我想要**看到核心指标的同环比变化趋势（如：对比昨日新增）**，以便于**评估近期推广投放策略的有效性**。

### 2.2 用户与订单管理 (客诉排查)
- **US3**: 作为**客服人员**，我想要**搜索和查看最近注册的用户列表及他们的第三方授权来源 (WeChat/Gmail)**，以便于**在收到反馈时能快速确认其账号状态**。
- **US4**: 作为**客服人员**，我想要**查看近期成功支付的回调订单流水（包含订单号、套餐类型、金额、支付时间）**，以便于**处理“已扣款但额度未到账”的客诉并进行人工对账补偿**。

### 2.3 系统安全与预警
- **US5**: 作为**研发/安全负责人**，我想要**监控 `/detect` 接口异常调用错误率 (如 5xx、调用 Sightengine 模型超时率)**，以便于**及时发现被刷单或上游 API 发生故障，触发熔断**。

---

## 3. 验收标准 (Acceptance Criteria, AC)

### 3.1 概览大盘 (Dashboard Overview)
- [ ] 后台首页必须呈现 4 个核心数据卡片 (KPI Cards)：Total Users, Revenue (CNY), Total Scans, Fake Images Ratio。
- [ ] 卡片数据必须从数据库 (`users`, `orders`, `scan_history` 表) 实时/近实时聚合而来。
- [ ] 卡片需要具备视觉反馈（如增长/趋势箭头标识或颜色层级）。

### 3.2 列表组件 (Data Components)
- [ ] **最新注册用户表**：至少显示用户首字母头像、姓名、注册渠道标签（Enum 样式）、注册时间，按时间倒序排列 (Limit 10)。
- [ ] **最新入账订单表**：清晰展示订单 ID 前缀、套餐名称 (Basic/Pro)、支付金额（从分转换为元）、准确的付款时间。需提供订单复制功能快捷键。

### 3.3 架构与访问权限
- [ ] API 必须收拢在 `/api/v1/admin/*` 路径。
- [ ] **[P0] 必须实装 Admin Token/Session 鉴权**，绝不允许将这些带有内部营收的裸接口暴露在公网，需支持通过环境变量或单一硬编码超管密码登录校验。
- [ ] 前端使用 React + Vite SPA 架构，分离于现有的 C 端 H5 工程，使用独立域名或独立端口配置。

---

## 4. 不在范围内 (Out of Scope - Phase 1)
为了控制开发周期（1.0 敏捷冲刺），以下高级功能此版本**不做**：
- **不做**：深度的折线图/柱状图表库接入（如 ECharts 日活留存漏斗图，一期仅做数值大盘）。
- **不做**：后台的用户编辑/删除、人工主动加减额度（CRUD 里的 UD 暂缓，一期仅做 Read 只读）。
- **不做**：细粒度的 RBAC 后台角色权限模型（超级管理员、客服、财务分权管理）。

---

## 5. 异常与边界状态 (Edge Cases & Error Handling)

| 场景 | 后端 / 前端 应对机制 |
| :--- | :--- |
| **数据库统计查询超时 (Timeout)** | 并发访问聚合语句 (COUNT, SUM) 时，若突破 5 秒，降级返回缓存中的昨日快照数据，或提示 `Data syncing...` |
| **遇到除以 0 的计算 (如总检测=0时求比例)** | 必须做安全保护 `fake_ratio = total > 0 ? fake / total : 0`，避免返回 `NaN` 导致前端崩溃。|
| **未授权访问管理系统 URL** | 前端响应拦截器抓取 `401 Unauthorized`，直接重定向清退回 `/login` 面板。|
