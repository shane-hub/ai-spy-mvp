# AI Spy - 产品需求文档 (PRD): 登录与支付模块升级

## 1. 背景与目标 (Background & Goals)
目前 AI Spy 核心链路（拍照/上传图片进行 AI 检测）已经跑通并跑在了 Flutter 前端与 Node.js 验证后端之上。但当前系统缺乏用户身份标识与商业化闭环，导致无法有效留存用户并进行商业变现。

**核心目标：**
- **构建账号体系**：引入用户登录模块，实现请求频控、历史记录云端同步等功能。
- **跑通商业化闭环**：引入支付模块（内购 IAP / 第三方支付），实现“免费体验 + 付费解锁高级功能/增加使用次数”的健康商业模式。
- **成功指标**：上线后实现 20% 的用户注册转化率，以及 2% 的付费转化率。

---

## 2. 用户故事 (User Stories)

### 2.1 登录注册
- **US1**: 作为**新访客**，我想要**在不登录的情况下免费体验 1 次 AI 检测**，以便于**建立对产品的信任并感受核心价值（Aha Moment）**。
- **US2**: 作为**免费用完 1 次的新访客**，我想要**通过 微信 / Gmail 一键登录获取额外 3 次免费额度**，以便于**促使我完成注册转化**。
- **US2**: 作为**老用户**，我想要**在换手机后使用 微信 / Gmail 登录账号**，以便于**找回我的检测历史和剩余积分/会员资产**。

### 2.2 支付与商业化
- **US3**: 作为**免费用户（额度耗尽）**，我想要**查看清晰的付费方案（提供低价尝鲜包 1.99元/10次 与 高性价比包月 19.9元/不限量）**，以便于**降低我的首单决策门槛**。
- **US4**: 作为**付费用户**，我想要**在通过微信支付充值后立刻看到额度更新/立刻能查阅被锁定的深度报告**，以便于**无缝继续使用核心功能**。
- **US5**: 作为**免费白嫖用户（额度耗尽且拒绝付费）**，我想要**通过观看一次激励型视频广告恢复 1 次检测额度**，以便于**继续使用服务（同时为平台贡献广告收益兜底成本）**。
- **US6**: 作为**老用户**，我想要**偶尔收到系统 Push 提示“你的历史检测图鉴被查看/有新分析”**，以便于**唤醒我重新打开 App（提升留存与召回）**。

---

## 3. 验收标准 (Acceptance Criteria, AC)

### 3.1 登录模块 AC
- [ ] 启动页后判断 Token，若无有效登录态，则引导至体验主页（允许访客免登测试 1 次）。
- [ ] 支持 微信 登录和 Gmail (Google) 登录。**（注：相同第三方授权邮箱判定为同一用户映射）**。
- [ ] 后端正确派发 JWT，**需明确 Token 刷新机制及卸载重装时的旧 Token 废弃/黑名单策略**。
- [ ] 个人中心 (Profile) 能够展示当前登录用户的 ID 掩码、剩余免费次数、付费资产及历史检测图鉴入口。

### 3.2 支付模块 AC
- [ ] 引入微信支付 SDK 及后端验签逻辑。
- [ ] 支付页面 (Paywall) 必须包含：产品名称、清晰的价格、订阅周期、隐私政策与服务条款链接。
- [ ] 支付成功后，后端接收微信 Webhook 并对该用户写入对应权限/额度。**（核心要求：接收 Webhook 必须实现绝地幂等性，依赖 `order_no` 唯一索引，并支持主动查单防丢）**。
- [ ] **提供 Sandbox / Test 环境接口**：后端必须在一期暴露用于模拟支付回调的内部接口，供 QA 和 E2E 自动化测试验证。

---

## 4. 不在范围内 (Out of Scope - Phase 1)
为了控制范围蔓延（Scope Creep），保证一期快速上线，以下功能**不做**：
- **不做**：复杂的会员等级积分返利 / 分销裂变系统。
- **不做**：恢复购买 (Restore Purchases) 功能（虚拟资产通过微信/Google账号绑定）。
- **不做**：加密货币支付。
- **不做**：繁琐的密码找回与实名认证（以三方 Auth 为主）。

---

## 5. 异常与边界状态 (Edge Cases & Error Handling)

| 异常场景 | 系统应对方案 / 提示语 |
| :--- | :--- |
| **无网/弱网环境尝试登录或支付** | 拦截请求并 Toast 提示：“网络似乎开小差了，请检查连接后重试”。 |
| **微信环境受限拉不起拉权/支付** | 前端需具备降级策略：隐藏微信登录入口，仅提供邮箱验证码或 Gmail 登录兜底。 |
| **并发请求导致额度超卖** | 后端 `/api/v1/detect` 扣减额度时必须使用乐观锁或 Redis 原子减操作，防范请求并发击穿。 |
| **微信支付进行中用户杀掉 App** | 下次启动时检查未完结的本地交易订单，向后端重新发起验证收据的请求。 |
| **用户在后端 Webhook 到达前查询额度** | 前端支付成功回调后引入短促 Loading (动画效果)，并在后台启动最多 5 次、每次间隔 3 秒的长轮询，获取额度更新后再放行。 |
| **JWT 过期** | 拦截 401 状态码，若刷新 Token 失败自动静默登出，跳转回登录页。 |
