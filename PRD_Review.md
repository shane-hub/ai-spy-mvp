# 📑 PRD 多角色技术与质量评审纪要 (Cross-functional PRD Review)

基于目前 `PRD.md` 中定义的“微信/Gmail登录”与“微信支付”需求，以下是来自 **后端架构师 (Backend)**、**前端架构师 (Frontend)** 以及 **质量保证专家 (QA)** 三个视角的评审意见与潜在风险提示：

---

## ⚙️ 1. 后端架构师 (Backend Dev) 评审意见

**原则：高并发、安全底线、异常兜底**

1. **登录鉴权机制 (Auth & JWT)**
   - **风险点**：PRD 中提到后端派发 JWT。我们需要明确 JWT 的有效期（如 access_token 2小时，refresh_token 7天）。如果客户端卸载重装，原有的 refresh_token 是否需要支持黑名单/作废机制？
   - **补充建议**：后端表结构需要设计 `users` 表，绑定 `wechat_openid` / `google_sub_id` 作为联合主键。为了防止重放攻击，登录接口设计需做基础的限流 (Rate Limiting)。

2. **微信支付回调 (Webhook) 幂等性**
   - **风险点**：微信支付服务器可能会对同一个订单发送多次成功回调（由于网络抖动）。
   - **补充建议**：后端接收 Webhook 必须做到**绝对的幂等性 (Idempotency)**。数据库层需通过建立 `order_no` 的唯一索引来防止多次加额度的发卡事故。同时，需要支持主动向微信网关发起“查单接口”，以应对丢失回调的情况。

---

## 🌐 2. 前端架构师 (Frontend Dev) 评审意见

**原则：状态容错、极佳的用户体验与边缘降级**

1. **三方登录 SDK 的接入与审核**
   - **风险点**：Flutter 接入微信登录需要在微信开放平台注册并配置 iOS/Android 的 Universal Links 和 App Signatures。前期配置如果卡住，会严重影响一期上线。
   - **补充建议**：考虑到国内/国外网络环境，能否在前端做异常降级？例如拉不起微信客户端时，隐藏微信登录入口，仅展示 Gmail（针对海外用户）或兜底的邮箱验证码登录。

2. **支付状态的轮询与 Optimistic UI**
   - **风险点**：PRD中提到的“前端支付成功回调后放行一次检测”，在实际场景中如果网络极差或后端回调长达1分钟未到，用户强杀 App，那次“放行”的额度如何记账？
   - **补充建议**：前端不应该完全采取无验证的乐观放行。建议方案：微信拉起支付返回成功后，前端触发一个短暂的 Loading（例如展示动画），并在后台开启 3 秒一次（最多轮询 5 次）的 HTTP 长轮询查询用户的最新余额。

---

## 🛡️ 3. 质量保证专家 (QA) 评审意见

**原则：破坏性测试、边界探索、回归拦截**

1. **测试数据的准备与沙盒环境**
   - **风险点**：微信支付的测试在正式上线前极难模拟真实的退款和网络中断。
   - **补充建议**：后端必须在一期就开放 `Sandbox` / `Test` 环境配置。QA 需要后端配合提供一个模拟回调的内部接口，用于在 UI 自动化脚本 (例如基于 Flutter integration_test) 中直接触发支付成功的 Webhook。

2. **账号资产(额度)并发扣减边界 (Race Condition)**
   - **核心破坏性测试**：当一个用户仅剩 1 次额度时，如果他用两台手机（或双开应用）在极短的时间内（毫秒级）同时点击“检测图片”，后端的乐观锁或 Redis 扣减逻辑是否能防住“超卖”？
   - **补充建议**：这是本次测试的重中之重。Dev 需要在 PRD 中明确写入“防并发扣费机制”，QA 将使用 JMeter 针对 `/api/v1/detect` 进行多线程并发压测。

3. **异常 Case 漏斗 (补充 PRD 中未提及的情况)**
   - **问题 1**：如果用户绑定了 Gmail，后来又想用用同一个收件邮箱的微信登录，系统是判定为同一个账号建立映射，还是提示邮箱已占用？
   - **问题 2**：支付未完成直接取消，是否有任何埋点用于漏斗分析？

---

### 📝 结论转交 (Summary for PM)
目前 PRD 大方向清晰，但我们建议在 PRD 中**补充以下三点细节**，一旦确认，各端即可无缝进入开发排期：
1. [Backend] 明确 JWT 过期刷新策略与 Webhook 幂等性约定。
2. [Frontend] 明确微信未安装/拉起失败情况下的 UI 降级策略。
3. [QA] 明确同邮箱/手机号在不同第三方登录时的合并或排斥策略。
